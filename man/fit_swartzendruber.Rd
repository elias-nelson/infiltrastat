% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_swartzendruber.R
\name{fit_swartzendruber}
\alias{fit_swartzendruber}
\title{Fit the Swartzendruber Infiltration Model}
\usage{
fit_swartzendruber(data = NULL, time, rate, na.rm = TRUE)
}
\arguments{
\item{data}{A data frame containing infiltration observations.}

\item{time}{Unquoted column name representing cumulative time
(numeric, non-negative; typically minutes).}

\item{rate}{Unquoted column name representing observed infiltration rate
(mm/hr or cm/hr).}

\item{na.rm}{Logical. If \code{TRUE} (default), incomplete observations
are removed before fitting.}
}
\value{
A list of class \code{"swartzendruber_model"} with components:
\itemize{
\item \code{model} — Model name.
\item \code{params} — Estimated parameters (\eqn{f_0}, \eqn{f_c}, \eqn{k}, \eqn{n}).
\item \code{fitted} — Data frame with:
\itemize{
\item \code{time} — elapsed time,
\item \code{observed} — measured infiltration rate,
\item \code{predicted} — fitted infiltration rate,
\item \code{residual} — residuals (observed – fitted rate),
\item \code{cumul_fit} — fitted cumulative infiltration (numerical integration).
}
\item \code{stats} — Goodness-of-fit statistics (RMSE, MAE, NRMSE,
PBIAS, R², NSE, AIC).
\item \code{nls_obj} — The fitted \code{nlsLM} model object.
}
}
\description{
Fits the empirical Swartzendruber infiltration model to observed
infiltration rate data using nonlinear least squares with the
Levenberg–Marquardt algorithm.
}
\details{
The Swartzendruber infiltration rate function is:

\deqn{f(t) = f_c + (f_0 - f_c) \exp(-k \cdot t^n)}

where:
\itemize{
\item \eqn{f(t)} — instantaneous infiltration rate,
\item \eqn{f_0} — initial infiltration rate,
\item \eqn{f_c} — steady infiltration rate,
\item \eqn{k} — decay constant,
\item \eqn{n} — shape parameter,
\item \eqn{t} — time.
}

Cumulative infiltration is approximated numerically by integrating
the fitted rate curve over time.

The Swartzendruber model generalizes Horton’s exponential decay by
introducing a shape parameter (\eqn{n}) to improve flexibility in
fitting infiltration curves. Cumulative infiltration is computed
numerically from the fitted rate curve. Starting values are estimated
internally: \eqn{f_0} from the maximum observed rate, \eqn{f_c} from
late-time rates, and default values for \eqn{k} and \eqn{n}.
}
\examples{
# Example dataset
infilt_data <- data.frame(
  Time = c(5, 10, 15, 20, 30, 45, 60),
  Rate = c(40, 28, 20, 15, 12, 10, 9)  # mm/hr
)

# Fit the Swartzendruber model
sw_fit <- fit_swartzendruber(data = infilt_data, time = Time, rate = Rate)

# Inspect estimated parameters
sw_fit$params

# Plot observed vs fitted infiltration rate
with(sw_fit$fitted, {
  plot(time, observed, pch = 16, col = "blue",
       xlab = "Time (minutes)", ylab = "Infiltration rate (mm/hr)")
  lines(time, predicted, col = "red", lwd = 2)
  legend("topright", legend = c("Observed", "Fitted"),
         col = c("blue", "red"), pch = c(16, NA), lty = c(NA, 1))
})

# Plot fitted cumulative infiltration curve
with(sw_fit$fitted, {
  plot(time, cumul_fit, type = "l", col = "red", lwd = 2,
       xlab = "Time (minutes)", ylab = "Cumulative infiltration (mm)")
})
}
\references{
Swartzendruber, D. (1969). Infiltration of water into soils.
\emph{Water Resources Research}, 5(2), 452–460.
}
